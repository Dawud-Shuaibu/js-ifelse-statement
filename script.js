// "ifelse" statement

let friend = 'not busy';
let weather = 'nice';

// Checking if friend is not busy and weather is nice
if (friend === 'not busy' && weather === 'nice') {
    console.log('He\'ll\ come tommorow');
    
// If friend is busy
} else if (friend === 'is busy') {
    console.log('He can\'t\ come tommorow');
}